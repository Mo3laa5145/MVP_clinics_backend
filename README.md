# clinica backend

A TypeScript backend starter using Express and PostgreSQL.

## Scripts

- npm install
- npm run dev
- npm run build

## Database

- Copy .env.example to .env if needed.
- Start PostgreSQL locally with Docker:
  - docker compose up -d postgres

## Health check

- http://localhost:3000/health
